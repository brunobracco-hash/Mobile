org.gradle.jvmargs=-Xmx3072m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# Assinatura do APK de release.
# Esta chave NAO e secreta: existe apenas para que atualizacoes do app
# instalem por cima da versao anterior sem erro de assinatura.
# O app nao vai para a Play Store e nao usa nenhuma credencial real.
P2W_STORE_FILE=../keystore/release.jks
P2W_STORE_PASSWORD=pdf2word
P2W_KEY_ALIAS=pdf2word
P2W_KEY_PASSWORD=pdf2word
